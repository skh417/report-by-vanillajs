"use strict";

const navMenus = document.querySelectorAll(".nav-menus li"); // nav 메뉴리스트
const section = document.querySelectorAll(".report");
const menuUnderbar = document.querySelector(".menu-under-bar"); // 메뉴 언더바
const extensionButtons = document.querySelectorAll(".extension-btn"); // 펼쳐보기,감추기 버튼
